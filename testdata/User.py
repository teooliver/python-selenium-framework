from mimesis import Person

# use faker to generate random users

# needs it should generate valid and invalid fields

class User:
    user_name: str
    first_name: str
    last_name: str
    email: str
    password: str
    password2: str
